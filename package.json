{
  "name": "britium-express-platform",
  "private": "true",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "lint": "echo \"Add ESLint command per app\"",
    "typecheck": "echo \"Run typecheck inside each app\"",
    "dev": "npm run dev --workspace apps/admin-web",
    "build": "npm run build --workspace apps/admin-web",
    "start": "npm run start --workspace apps/admin-web"
  },
  "devDependencies": {
    "@types/node": "^25.5.0",
    "@types/qrcode": "^1.5.6",
    "@types/react": "^19.2.14",
    "supabase": "^2.84.4",
    "typescript": "^5.9.3"
  }
}
